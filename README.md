Sources for the plugin `plugin.fbAudienceNetwork`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.fbAudienceNetwork" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
